# INC-001 — Database Connectivity

**Severity:** HIGH

**Symptom:** Order API returned HTTP 500.

**Investigation:** Application logs showed database connection timeout errors.

**Likely root cause:** Database connectivity or connection-pool problem.

**Recommended resolution:** Verify database availability, connection configuration and recent SQL/database changes.

**Validation:** Confirm the API returns a successful response after the issue is corrected.
